git commands 

1.Create a directory and create a file.
2.create git repository.
3.to initialize git repository we write the command called "git init"
4.In order to track changes  we use "git status" command
5.in order to add it to staging area and track changes in it we use a command 
called "git add" command.
6.to commit the changes we use command called (git commit -m "complete chapter1")
7. to see the commited details we use "git log" to log all he commited details like date time.
8.  to open a file we use a command called "code filename.txt".
9. to add multiple files at once we use a command called "git add ."

